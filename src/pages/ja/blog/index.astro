---
import { getCollection } from 'astro:content';
import Layout from '../../../layouts/Layout.astro';
import Newsletter from '../../../components/Newsletter.astro';

const allPosts = await getCollection('blog', ({ data }) => data.lang === 'ja');
const posts = allPosts.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

const title = 'Pilgrimage Blog - Mount Kailash Guide & Stories | Kailash 2026';
const description = 'Mount Kailash pilgrimage blog: latest kora guides, Horse Year updates, pilgrim stories, and in-depth cultural insights. Professional guidance for your sacred journey.';
---

<Layout title={title} description={description} lang="en">
  <!-- Hero -->
  <section class="relative bg-kailash-dark">
    <div class="relative w-full h-[35vh] lg:h-[40vh]">
      <div class="absolute inset-0 bg-[url('/images/kailash-aerial.jpg')] bg-cover bg-center opacity-60"></div>
      <div class="absolute inset-0 bg-gradient-to-b from-kailash-dark/40 via-transparent to-kailash-dark"></div>
    </div>
    <div class="relative -mt-12 z-10 px-4 pb-8">
      <div class="max-w-4xl mx-auto text-center">
        <p class="text-kailash-gold text-sm tracking-[0.3em] uppercase mb-3 font-light">Knowledge & Deep Guides</p>
        <h1 class="font-display text-4xl md:text-5xl font-bold text-white mb-3">Pilgrimage Blog</h1>
        <p class="text-lg text-white/60 max-w-2xl mx-auto">
          Expert Mount Kailash kora guides, Horse Year updates, and in-depth cultural insights
        </p>
      </div>
    </div>
  </section>

  <!-- Blog Posts Grid -->
  <section class="py-16 bg-kailash-light">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      {posts.length === 0 ? (
        <div class="text-center py-20">
          <p class="text-gray-500 text-lg">More articles coming soon...</p>
        </div>
      ) : (
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
          {posts.map((post) => {
            const slugParts = post.id.split('/');
            const cleanSlug = slugParts.length > 1 ? slugParts.slice(1).join('/').replace(/\.md$/, '') : slugParts[0].replace(/\.md$/, '');
            const formattedDate = post.data.date.toLocaleDateString('en-US', {
              year: 'numeric',
              month: 'long',
              day: 'numeric',
            });

            return (
              <article class="bg-white rounded-2xl overflow-hidden shadow-md hover:shadow-xl transition-all duration-300 hover-lift group">
                <a href={`/en/blog/${cleanSlug}`} class="block">
                  <div class="relative aspect-[16/10] overflow-hidden bg-gray-100">
                    <img
                      src={post.data.image}
                      alt={post.data.title}
                      class="absolute inset-0 w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
                      loading="lazy"
                    />
                    <div class="absolute inset-0 bg-gradient-to-t from-black/40 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                  </div>
                </a>
                <div class="p-6">
                  <div class="flex flex-wrap gap-2 mb-3">
                    {post.data.tags.slice(0, 3).map((tag) => (
                      <span class="text-xs bg-kailash-cream text-kailash-dark/70 px-2.5 py-1 rounded-full font-medium">
                        {tag}
                      </span>
                    ))}
                  </div>
                  <a href={`/en/blog/${cleanSlug}`} class="block group">
                    <h2 class="font-display text-xl font-bold text-kailash-dark mb-3 group-hover:text-kailash-gold transition-colors leading-snug">
                      {post.data.title}
                    </h2>
                  </a>
                  <p class="text-gray-600 text-sm line-clamp-3 mb-4 leading-relaxed">
                    {post.data.description}
                  </p>
                  <div class="flex items-center justify-between">
                    <time datetime={post.data.date.toISOString()} class="text-sm text-gray-400">
                      {formattedDate}
                    </time>
                    <a
                      href={`/en/blog/${cleanSlug}`}
                      class="text-kailash-gold font-medium text-sm hover:text-kailash-accent transition-colors inline-flex items-center gap-1"
                    >
                      Read more
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                      </svg>
                    </a>
                  </div>
                </div>
              </article>
            );
          })}
        </div>
      )}
    </div>
  </section>

  <!-- Newsletter -->
  <section class="py-16 bg-kailash-dark">
    <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8">
      <Newsletter lang="ja" variant="card" />
    </div>
  </section>
</Layout>
